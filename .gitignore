# Local Claude Code settings (machine-specific)
.claude/settings.local.json
CLAUDE.md

# Go build output
/bin/
/dist/
*.exe
*.test
*.out
coverage.*

# Built binaries in repo root (go build ./cmd/... output)
/apid
/cpaneld
/reportd
/dmarcpulld
/dmarcd
/dnsd
/correld
/repd
/incidentd
/aid
/abused
/rbld
/anomalyd
/fbld
/authwatchd
/ingestd
/telemetryd
/mxctl

# Runtime artifacts
/dmarc-drop/
telemetry-spool.ndjson

# Frontend (Next.js)
/web/node_modules/
/web/.next/
/web/out/

# Env / secrets — never commit (deploy/.env, .env, etc.) but keep the example templates
.env
.env.*
!.env.example
!.env.prod.example

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# local build artifacts
/cpanel-plugin
/mxsentinel-cpanel-plugin.tar.gz
/mxsentinel-cpanel-plugin.zip
